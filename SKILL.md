---
name: modelarts-glm-deploy-bench
description: "在华为云/HCSO ModelArts Standard 上部署、排障和调优 Ascend 大模型（含 GLM/Flash），先确认用户场景与完整压测矩阵，再通过 LiteLLM/AIS-bench 做可续跑性能及功能测试，低频定时检查并自动生成分配置 Excel。环境参数化，不依赖特定电脑、账号或历史项目。"
---

# ModelArts 通用大模型部署、调优与测试

把部署产物、实际运行态、测试原始记录和结论连成可追溯链。适用 ModelArts Standard；模型、主机、卡数、路径、认证与报告位置均由本次环境提供，不继承任何历史项目状态。技能名称保留以兼容已有调用方式，工作流可用于框架支持的其他模型。

## 先确定本次工作

先读用户最新要求和本次项目 checkpoint（如有），只读交接记录的相关尾部，不加载整段历史。区分：只整理结果、诊断、准备脚本、部署变更、实际压测、监控。仅问进展或要报告不授权启动服务；调用本技能也不恢复旧任务。暂停/停止指令优先于旧自动化和续跑计划。

环境信息可从用户提供的证据恢复；开始新测试前必须向用户确认测试场景与展开后的矩阵，已明确确认的内容不重复问。要确认：

- 精确模型/权重版本、量化、硬件型号及卡数、Standard 服务/部署/版本。
- 本次允许变更哪些服务，哪些只能启停，哪些完全保护；卡数、停机与测试时限。
- 已选推理框架/官方镜像、上下文总上限、输出策略、LiteLLM 路由。
- 目标场景、质量门槛、TTFT/TPOT 的统计量、缓存条件、结果保存位置。

环境配置使用本次的 build_host、load_host、inference_targets、protected_services、model_source、image_source、artifact_root、handoff_path、可选notes_destination及secret_ref。角色可在同一或不同机器，必须实际核对；不猜主机名/IP/本机目录。基线服务与新候选隔离路径、版本和名称。共享代理/监控全局配置不属于默认调优范围，历史启停许可不自动延续。

## 测试的六条必做规则

1. **先问场景，再确认完整矩阵。** 询问是coding、对话、检索等，以及并发/输入范围、输出与时间预算。范围要展开成笛卡尔积，不能只测两端。例如并发1–64、输入8K–256K：并发 `[1,4,8,16,32,64]` × 输入 `[8,16,32,64,128,256]K`，每个配置共 **36格**。向用户列出档位、数量和预计耗时再开始。不同配置或额外冷热模式另计。
2. **coding重点测decode，热缓存门槛≥98%。** 原生token口径验证所有实际副本的缓存覆盖及测量区间命中率；未达到或无法验证不能标为合格coding热测。长输入保留，生成自然完整代码，TTFT仍记录。冷测有需要则单独列出，不冒充98%热测。
3. **测试终止后自动产出Excel。** 不等用户再要报告。每个不同配置独立sheet，列是输入、行是并发，至少包含TTFT、TPOT、单请求/整批TPS、NPU利用率、HBM占用率矩阵和完成状态。失败/缺测照实保留，具体单位和资源汇总口径见benchmark参考。
4. **功能题每个客户场景5–10道。** 选中等难度、运行时间较短的不同题，预先固定题库/判分/时限，不能用同一题重复多次充数；先与用户确认具体题数和预计总时长。
5. **先完成脚本，再启动与设定时任务。** 在发正式负载前写完矩阵执行、资源采集、断点恢复、汇总和Excel生成脚本，完成离线校验；启动后创建低频定时检查，默认建议10分钟。脚本自行跑完，不依赖GPT持续监控；完成/异常时再读取摘要和整理结果。
6. **通用且可移植。** 技能不含个人机器、账号、密码、私钥、历史实验路径/成绩或专用脚本依赖。SFS/Obsidian只是可配置目的地，不要求本机安装或固定挂载。

## 按任务读取

| 当前工作 | 必读参考 |
|---|---|
| 权重、镜像、Standard 启动、混部或 PD | [deployment.md](references/deployment.md) |
| 参数调优、定位性能瓶颈 | [tuning.md](references/tuning.md)，以及 benchmark 的统计口径 |
| AIS-bench、客户题、SWE、历史结果矩阵 | [benchmark.md](references/benchmark.md) |
| 修改测试器、后台运行、断点续跑或写交接 | [resume-handoff.md](references/resume-handoff.md) |

展开矩阵可用本技能的 [build_matrix_plan.py](scripts/build_matrix_plan.py)。它仅输出计划JSON，不调用模型、不启动服务；将用户确认后的计划保存进实验manifest。

只读当前模式需要的参考。用到的外部 API/参数需查当时官方文档与镜像内实际版本；不要把参考中的历史 flag 当成新镜像兼容保证。不要为了写技能或报告连接生产环境。

## 执行与验收边界

1. 只读核对实际 ECS/服务身份、运行版本、卡资源归属、活动测试。连接成功不等于连接到目标机器。
2. 变更前保存原配置、脚本 hash、镜像摘要和回退路径；记录变更意图。准备与实际提交分别记账。
3. 离线检查之后，仅在本次授权内提交。用新 cohort 的启动参数、初始化日志、设备与健康检查确认运行态；不能以 UI“运行中”、脚本 echo 或离线测试替代。
4. 小请求通过所选 LiteLLM 路由验证目标后端，再按已确认矩阵压测。同一资源池不叠加无关基准。失败后先确认旧请求排空，再决定是否继续。
5. 原始记录先落盘，汇总后检查计数、token、单位、分母和配置一致性，自动生成Excel，再写本次配置的交接位置（可选SFS/Obsidian）。明确哪些只是候选、启动成功、性能通过、质量通过或仍未测试。

运行配置变化、依赖变化和测试方案变化须分版本，不能在运行中的脚本上热改，也不能拼成一个成绩。优先质量不退化，然后在同场景下比较 TTFT/TPOT；不通过压短输出、隐藏推理、改题、开 fallback、只算成功者来“达标”。

技能不保存密码、API key、私钥、OTP、登录 cookie。可使用用户授权的现有连接/凭据引用，遵循当前工具的认证约束；需 MFA 时请求当次验证码，不复用旧码、不扩大安全组、不关闭认证。认证受阻时继续能做的离线工作，并具体说明受阻操作，勿把局部受阻说成所有工作都做不了。

## 结束时

报告实际完成的步骤、当前版本、可验证结果、Excel、失败/缺测及下一步。只整理历史时不恢复服务、不追加测试。新测试使用产品提供的定时机制（受当前工具权限约束），终止后暂停/删除该次检查任务；写或更新本技能时不实际创建测试定时任务。
